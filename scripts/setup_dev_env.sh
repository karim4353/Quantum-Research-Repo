
#!/usr/bin/env bash
set -e
echo "This script helps setup a dev env on Ubuntu (example)."
echo "Install Python, pip, cmake. For LLVM optional:"
echo "  sudo apt install -y llvm-dev libclang-dev clang"
echo "The repo supports --sim mode if LLVM/PyTorch not installed."
